package com.neaterbits.compiler.java.parser;

enum JavaStatement {
	VARIABLE_DECLARATION,
	ASSIGNMENT,
	EXPRESSION,
	ASSERT,
	IF_THEN,
	IF_THEN_ELSE,
	SWITCH,
	WHILE,
	DO,
	FOR,
	
	TRY_CATCH,
	TRY_CATCH_FINALLY,
	TRY_WITH_RESOURCES,
	
	RETURN;
}
