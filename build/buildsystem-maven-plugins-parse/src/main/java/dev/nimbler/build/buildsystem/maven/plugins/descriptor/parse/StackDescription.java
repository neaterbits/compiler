package dev.nimbler.build.buildsystem.maven.plugins.descriptor.parse;

import org.jutils.parse.context.Context;

import dev.nimbler.build.buildsystem.common.parse.StackTexts;

final class StackDescription extends StackTexts {

    StackDescription(Context context) {
        super(context);
    }
}
