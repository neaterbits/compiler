package com.neaterbits.build.buildsystem.maven.plugins.descriptor.parse;

import com.neaterbits.build.buildsystem.common.parse.StackTexts;
import com.neaterbits.util.parse.context.Context;

final class StackDescription extends StackTexts {

    StackDescription(Context context) {
        super(context);
    }
}
