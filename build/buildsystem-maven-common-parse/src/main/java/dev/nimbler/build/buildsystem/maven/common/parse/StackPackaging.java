package dev.nimbler.build.buildsystem.maven.common.parse;

import com.neaterbits.util.parse.context.Context;

import dev.nimbler.build.buildsystem.common.parse.StackText;

final class StackPackaging extends StackText {

    StackPackaging(Context context) {
        super(context);
    }
}
