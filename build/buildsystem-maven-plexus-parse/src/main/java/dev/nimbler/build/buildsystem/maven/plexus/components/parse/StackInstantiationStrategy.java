package dev.nimbler.build.buildsystem.maven.plexus.components.parse;

import org.jutils.parse.context.Context;

import dev.nimbler.build.buildsystem.common.parse.StackText;

final class StackInstantiationStrategy extends StackText {

    StackInstantiationStrategy(Context context) {
        super(context);
    }
}
