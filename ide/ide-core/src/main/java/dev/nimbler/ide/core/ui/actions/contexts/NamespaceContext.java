package dev.nimbler.ide.core.ui.actions.contexts;

import dev.nimbler.build.types.resource.NamespaceResourcePath;

public class NamespaceContext extends ResourceContext<NamespaceResourcePath> {

	public NamespaceContext(NamespaceResourcePath resource) {
		super(resource);
	}
}
